"""Manual login helper — no longer needed with Browserbase.

Browserbase handles CAPTCHAs and browser fingerprinting automatically.
Login is performed via Stagehand act() on every session.

This file is kept as a stub so existing imports don't break.
"""
